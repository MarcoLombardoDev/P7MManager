# Development dependencies. The test-suite signs its own fixtures with the
# openssl binary, which is a system tool rather than a Python package: where it
# is missing the CMS tests skip and the rest still run.
-r requirements.txt
pytest>=8.0
ruff>=0.5
Pillow>=10.0    # tools/make_icon.py, and the test that checks the committed icons

# tests/test_release_workflow.py parses the workflow files. It imports PyYAML
# outright rather than skipping without it: those checks guard the workflow
# that publishes the releases, and a run that silently omitted them would
# finish green while proving nothing.
pyyaml>=6
